#include "main.h"

/**
 * print_line -  checks for checks for a digit (0 through 9).
 * @n: n -  Variable
 * Return: Always 0.
 */
void print_line(int n)
{
	char c

		for (c = 0; c < n; c++)
		{
			_putchar('_');
		}
	_putchar('\n');
}
