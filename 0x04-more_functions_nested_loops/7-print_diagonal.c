#include "main.h"
/**
 * print_diagonal -  checks for checks for a digit (0 through 9).
 * @n: n -  Variable
 *
 * Return: Always 0.
 */
void print_diagonal(int n)
{
	char c;

		for (c = 0; c < n; c++)
		{
			_putchar('\');
		}
	_putchar('\n');
}
